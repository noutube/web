<ModalDialog @onClose={{@close}} @translucentOverlay={{true}} @containerClass={{local-class "app-dialog"}}>
  {{#if @title}}
    <div local-class="top">
      {{@title}}
    </div>
  {{/if}}

  <div local-class="middle">
    {{yield}}
  </div>

  {{#if @accept}}
    <div local-class="bottom">
      <button type="button" {{on "click" @accept}}>{{if @acceptLabel @acceptLabel "Yes"}}</button>
      <button type="button" {{on "click" (if @cancel @cancel @close)}}>{{if @cancelLabel @cancelLabel "No"}}</button>
    </div>
  {{else if @close}}
    <div local-class="bottom">
      <button type="button" {{on "click" @close}}>Close</button>
    </div>
  {{/if}}
</ModalDialog>
