{{!-- can't override parent's separator, have to just replace whole title --}}
{{title this.titleNotification " NoUTube" replace=true}}

<PortalContent @for="menu-bar-extras">
  <button type="button" {{on "click" this.toggleSorting}} title="Sorting settings"><SvgIcon @name="sliders" /></button>
  <button type="button" {{on "click" this.settings.switchSize}} title="Switch player size"><SvgIcon @name="maximize" /></button>
</PortalContent>

{{#if this.showSorting}}
  <AppDialog @title="Sorting Settings" @close={{this.toggleSorting}}>
    <SortingSettings />
  </AppDialog>
{{/if}}

{{#if this.newSubscriptions}}
  <SectionHeader @icon="gift">New Videos</SectionHeader>
  {{#if this.settings.channelGroup}}
    <DisplaySubscriptions @subscriptions={{this.newSubscriptions}} @state="new" />
  {{else}}
    <DisplayUngrouped @items={{this.newItems}} />
  {{/if}}
{{/if}}

{{#if this.laterSubscriptions}}
  <SectionHeader @icon="bookmark">Watch Later</SectionHeader>
  {{#if this.settings.channelGroup}}
    <DisplaySubscriptions @subscriptions={{this.laterSubscriptions}} @state="later" />
  {{else}}
    <DisplayUngrouped @items={{this.laterItems}} />
  {{/if}}
{{/if}}

{{#unless this.anyItems}}
  <SectionHeader @icon="slash">No Videos</SectionHeader>
  <div class="message">
    <p>You've watched everything for now, check again later.</p>
  </div>
{{/unless}}
